/**
 * Vitest setup file — referenced by `vite.config.test.ts:setupFiles`.
 *
 * Loaded once before each test file. Currently empty (jsdom environment
 * is configured via the test config; @testing-library/svelte cleanup
 * runs automatically). Extend here when component tests start needing
 * shared mocks (e.g. `localStorage`, `EventSource`, `fetch`) or
 * matchers from `@testing-library/jest-dom`.
 */
